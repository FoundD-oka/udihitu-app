{
    "version": 2,
    "builds": [
      {
        "src": "/public/**",
        "use": "@vercel/static-build",
        "config": {
          "distDir": "out"
        }
      },
      {
        "src": "/src/**",
        "use": "@vercel/static-build",
        "config": {
          "distDir": "out"
        }
      }
    ],
    "installCommand": "npm install"
  }